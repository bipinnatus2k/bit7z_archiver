[package]
name = "bit7z-app-test"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bit7z-app-archive = { path = "../archive" }
bit7z-domain = { path = "../../domain" }
bit7z-infra-progress = { path = "../../infrastructure/progress" }

crc32fast = "1.4"
