[package]
name = "bit7z-app-test"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bit7z-domain = { path = "../../domain" }
bit7z-infra-progress = { path = "../../infrastructure/progress" }
crossbeam = "0.8"
crc32fast = "1.4"
