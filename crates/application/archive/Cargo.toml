[package]
name = "bit7z-app-archive"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bit7z-domain = { path = "../../domain" }

[dev-dependencies]
bit7z-domain = { path = "../../domain", features = ["testing"] }

