[package]
name = "bit7z-infra-events"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bit7z-domain = { path = "../../domain" }
bit7z-app-checksum = { path = "../../application/checksum" }
