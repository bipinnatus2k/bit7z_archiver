[package]
name = "bit7z-app"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform compressed file viewer and editor"

[dependencies]
bit7z-rt-gui = { path = "../gui" }
